// --------------------------------------------------------------------------
// 실습: 타입 강제 변환
// --------------------------------------------------------------------------
// * 자바스크립트는 연산 시, 자동으로 타입을 강제 변환합니다.
// * 명시적 변환(Explicit)과 암묵적 변환(Implicit)이 있습니다.
// --------------------------------------------------------------------------


// --------------------------------------------------------------------------
// 문자열로 강제 변환
// --------------------------------------------------------------------------

let x = 5

// 암묵적 변환: 숫자 + 문자열 → 문자열로 강제 변환
// 설명: 숫자가 문자열로 변환되어 연결됨
// 출력 결과: '55' (string)

// 명시적 변환: Number() 함수 사용
// 설명: 문자열을 숫자로 명시적 변환 후 덧셈
// 출력 결과: 10 (number)


// --------------------------------------------------------------------------
// 숫자로 강제 변환
// --------------------------------------------------------------------------

// 암묵적 변환: 숫자 * 문자열 → 숫자로 변환 (곱셈 연산)
// 설명: 문자열 '5'가 숫자 5로 자동 변환되어 곱셈
// 출력 결과: 25 (number)

// 암묵적 변환: 뺄셈은 숫자로 변환
// 출력 결과: 5 (number)

// 암묵적 변환: 나눗셈은 숫자로 변환
// 출력 결과: 5 (number)

// 암묵적 변환: 나머지 연산은 숫자로 변환
// 출력 결과: 10 (number)

// 핵심!
// 덧셈인 경우에만 숫자가 문자로 변환되고,
// 뺄셈, 곱셈, 나눗셈, 나머지 연산에서는 문자가 숫자로 변환됨


// --------------------------------------------------------------------------
// null의 강제 변환
// --------------------------------------------------------------------------

// 암묵적 변환: null + 숫자 → null은 0으로 변환 (falsy 값)
// 설명: null은 숫자 연산 시, 0으로 강제 변환됨
// 출력 결과: 5 (number)

// 명시적 변환: Number() 사용
// 설명: null을 명시적으로 숫자로 변환하면 0
// 출력 결과: 0 (number)

// 암묵적 변환: null 곱하기 연산
// 출력 결과: 0 (number)

// 암묵적 변환: null 나누기 연산
// 출력 결과: Infinity (number)

// 암묵적 변환: null 나머지 연산
// 출력 결과: NaN (number)


// --------------------------------------------------------------------------
// boolean의 강제 변환
// --------------------------------------------------------------------------

// 암묵적 변환: 숫자로 강제 변환
// true → 1로 변환
// 출력 결과: 6 (number)

// false → 0으로 변환
// 출력 결과: 5 (number)

// 명시적 변환: Number() 사용
// Number(true) → 1
// 출력 결과: 6 (number)

// Number(false) → 0
// 출력 결과: 5 (number)

// 암묵적 변환: boolean과 곱하기 연산
// true → 1로 변환
// 출력 결과: 5 (number)

// 출력 결과: 0 (number)
// false → 0으로 변환


// --------------------------------------------------------------------------
// undefined의 강제 변환
// --------------------------------------------------------------------------

// 암묵적 변환: undefined + 숫자 → NaN (Not a Number)
// 설명: undefined는 숫자로 변환할 수 없어 NaN이 됨
// 출력 결과: NaN (number)

// 명시적 변환: Number(undefined)
// 출력 결과: NaN (number)

// 암묵적 변환: undefined와 곱하기 연산
// 출력 결과: NaN (number)

// 암묵적 변환: undefined와 빼기 연산
// 출력 결과: NaN (number)


// --------------------------------------------------------------------------
// 실습 예제
// --------------------------------------------------------------------------

// 사용자 입력 처리 (문자열 → 숫자)
let user_age = '25'
// 출력 결과: 26 (number)
let next_year_age

// 계산 프로그램
let num1 = '110'
let num2 = '26'
// 출력 결과: 136 (number)
let sum

// 가격 계산
let price = '15000'
let quantity = 3
// 출력 결과: 45000 (number)
let total

// 할인율 계산
let original_price = 50000
let discount_rate = '0.2'  // 20% 할인
// 출력 결과: 40000 (number)
let final_price


// --------------------------------------------------------------------------
// 주의해야 할 경우
// --------------------------------------------------------------------------

// + 연산자는 문자열 연결 우선
x = '5' + 3 + 2
// 설명: 왼쪽부터 순서대로 '5' + 3 = '53', '53' + 2 = '532'
// 출력 결과: '532' (string)

// 다른 연산자는 숫자 변환 우선
x = '5' - 3 + 2
// 출력 결과: 4 (number)
// 설명: '5' - 3 = 2, 2 + 2 = 4

// 괄호로 명확하게 표현
x = Number('5') + 3 + 2
// 출력 결과: 10 (number)

// NaN 체크
x = 'abc' * 5
// 출력 결과: NaN (number)


// --------------------------------------------------------------------------
// Falsy와 Truthy 값
// --------------------------------------------------------------------------

// Falsy로 변환되는 값들: false, 0, '', null, undefined, NaN

// Falsy가 아닌 모든 값은 Truthy로 변환: 1, -1, '0', 'false', [], {}
